
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1>AddBooks</h1>

<h4>User</h4>
<p>
    <a asp-action="AddMyBooks">AddMyBooks New</a>
</p>
@*<div class="row">
    <div class="col-md-4">
        <form asp-action="AddMyBooks">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                @Html.DropDownList("user", new SelectList(new string[] { "Sasha", "Masha", "Dasha" }), "Выберите пользователя")

            </div>           
            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>*@
